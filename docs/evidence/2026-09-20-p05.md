# P05 changesets and projections — 2026-09-20

Ledger: pass at `2026-09-20T19-57-57.045Z-1dba2339`.
Bulky artifacts: `.artifacts/qualification/p05` (gitignored).

## Picks

- Open-changeset TTL: `86400000` ms (`CHANGESET_TTL_MS`, 24 hours). A commit
  whose `tx.pt` is past first-member `pt + TTL` expires; already-committed
  changesets stay visible after a later clock jump.
- Postgres dialect for the probe: `@effect/sql-pglite` (same as P04).
- Reactivity keys are string arrays `prefix:{id}` and `attribute:{name}` (Effect
  record keys would invalidate every query that used the same table name).
- Conflict facts live in `projection_conflicts` (not appended to the log). P09
  may persist conflict datoms at accept time.

## Vocabulary (D44)

Pinned in `libs/datom/src/vocabulary.ts` and re-exported by
`@viviefs/evidence-model`:

- `viviefs/changeset/commit`, `viviefs/changeset/abort`, `viviefs/conflict`
- `evidence/list`, `evidence/list/title`
- `evidence/item`, `evidence/item/title`, `evidence/item/seal`
- `evidence/captured`, `evidence/title`, `evidence/file`, `evidence/owner`

Engine journal attributes wait for P06. These names cannot be renamed or
retyped.

## Checks

Fourteen checks, including the incomplete-changeset positive control: incomplete
changeset, commit ordering, `cs == tx`, abort, expiry, basis LWW, basis
write-once, basis human conflict, defining-attribute lifecycle, prefix-id
cascade, orphan conflict, Reactivity keys, rebuild equals incremental, draft
view.

| Store | Host |
| --- | --- |
| sqlite-node | pass |
| postgres (PGlite) | pass |
| iOS simulator (op-sqlite) | pass |
| Android emulator `viviefs-p01` (op-sqlite) | pass |
| web (wasm + OPFS) | pass |

Android emulator is required; it passed.

## Read models

P05 pins disposable projection tables: `projected_facts`, `projection_conflicts`,
`changeset_status`, `projection_cursor`. Full rebuild from the log equals the
incremental snapshot.
