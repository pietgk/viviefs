# P04 log store conformance — 2026-09-20

Ledger: pass at `2026-09-20T19-30-57.292Z-6aa3906d`.
Bulky artifacts: `.artifacts/qualification/p04` (gitignored).

## Picks

- Future-skew bound: `5000` ms (`FUTURE_SKEW_MS`).
- Append volume: `2000` datoms (`APPEND_VOLUME`).
- Postgres dialect for the probe: `@effect/sql-pglite` (Apache-2.0).

## Checks

Eleven checks, including the duplicate-append positive control: append, duplicate
append, HLC mint, backwards clock, reboot, remote-ahead, future skew, cursor
streaming, prefix scans, compaction horizon, volume.

| Store | Host |
| --- | --- |
| sqlite-node | pass |
| postgres (PGlite) | pass |
| iOS simulator (op-sqlite) | pass |
| Android emulator `viviefs-p01` (op-sqlite) | pass |
| web (wasm + OPFS) | pass |

Android emulator is required; it passed.

## Columns

P04 pins SQL columns: `datoms(e, a, v, tx, op, cs)` plus `seq`, `changesets`,
`hlc_state`, `device_cursors`. Attribute names stay unpinned (D44).
