# P06 workflow engine crash matrix — 2026-09-20

Ledger: pass at `2026-09-20T20-52-39.334Z-f904c91d`.
Bulky artifacts: `.artifacts/qualification/p06` (gitignored).

P06 is engine-level (TestClock-free host kills at journal boundaries). Device
force-quit and notification wake are P07.

## Picks

- Journal attributes (D44): `viviefs/workflow/started`, `viviefs/workflow/result`,
  `viviefs/activity/exit`, `viviefs/deferred/exit`, `viviefs/clock/wake-at`,
  `viviefs/lease/holder`.
- Id shapes: `O{org}/W{exec}`, `/A{name}#{attempt}`, `/D{name}`, `/C{name}`
  (`/` in names encoded as `~`).
- Durable clock in the probe: 150 ms with `inMemoryThreshold: 0` so the clock
  always goes through `scheduleClock`.
- Host stores: sqlite-node and Postgres via PGlite (same as P04/P05). Device
  force-quit is [P07](2026-09-21-p07.md).

## Checks

Seven kill boundaries on each host store, plus the memory-engine positive
control: kill before an activity, during it, after it before the journal write,
during a deferred wait, during a clock, during a lease handoff, and during a
content-hash upload. Every datom-backed case resumed; each external effect was
observable at most once via activity idempotency keys. After a killed lease
handoff the original device still held epoch 1; a completed handoff to device B
was epoch 2 and the stale device died on `deferredDone`.

| Store | Host |
| --- | --- |
| sqlite-node | pass |
| postgres (PGlite) | pass |
| `WorkflowEngine.layerMemory` | pass (positive control: poll is none after kill, activities re-run) |

`engine.ts` is 646 lines, slightly over the 300-600 estimate.

`crypto.subtle.digest` via `Effect.tryPromise` does not complete inside a
Workflow activity fiber. The harness hashes `p06-file` once; the journaled
upload activity records `blob:{sha256}` with an idempotency key.
