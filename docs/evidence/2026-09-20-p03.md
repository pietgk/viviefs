# P03 OTLP and motel — 2026-09-20

Ledger: pass at `2026-09-20T18-30-20.436Z-58dbe271`.
Bulky artifacts: `.artifacts/qualification/p03` (gitignored).

## Pin

- `@kitlangton/motel@0.2.8` (MIT), run from `vendor/motel` with bun 1.4.2.
- motel's own Effect line is `4.0.0-beta.90`. bun otherwise resolves
  `@effect/platform-node-shared` to the workspace RC and the daemon exits
  (`effect/ByteSize` / `NetAddress` missing). Overrides pin Effect and
  `@effect/platform-*` to beta.90. That is a packaging pin, not a change
  to the app's Effect RC.

## Checks

The app emits `p03.known-span` with a unique `probe.token` through
`otlpJsonLayer` (`OtlpTracer` + JSON + `fetch`). motel is queried at
`/api/spans/search?service=viviefs-evidence-p03&attr.probe.token=…`.

| Platform | Motel ingest | Wrong endpoint |
| --- | --- | --- |
| iOS simulator | span arrived | no crash, no motel row |
| Android emulator (`viviefs-p01`, `adb reverse` 27686) | span arrived | (iOS was the control) |

Reload in the harness can emit the span twice; the gate requires at least
one hit.

## Physical device

Documented, not run: [p03-physical-device.md](p03-physical-device.md)
(LAN binding or cursor catch-up).
