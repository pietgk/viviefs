# P07 device resume E2E — 2026-09-21

Ledger: pass at `2026-09-21T07-05-26.784Z-33ca482f`.
Bulky artifacts: `.artifacts/qualification/p07` (gitignored).

P07 is process-kill on a real iOS simulator and Android emulator. The host
launch-sweep checks (resume vs skip) ran on sqlite-node; P06 already covered
journal-boundary kills.

## Picks

- Park marker (probe-only, D44): `viviefs/probe/parked`.
- Exemplar workflow: `DeviceResume.v1` (upload activity, `approval` deferred).
- Notification title: `P07 approval`. Android channel `viviefs-p07-alarm`
  (`expo-notifications` 58.0.3, MIT).
- Launch resume is an explicit `EngineSweep` call (D3, D24). The engine does
  not sweep by itself.
- Motel service: `viviefs-evidence-p07`. Spans `p07.park` and `p07.resume`.

## Checks

Force-quit mid-upload and mid-wait on iOS simulator and Android emulator;
relaunch with sweep resumes; without sweep the workflow stays parked
(abandoned). Motel received park and resume spans for the mid-upload token on
both platforms. Completing the approval deferred after relaunch finished
`DeviceResume.v1` with `blob:p07`.

| Platform | mid-upload resume | mid-wait deferred | without sweep | park+resume spans |
| --- | --- | --- | --- | --- |
| sqlite-node (host) | pass (sweep vs skip) | n/a | pass | n/a |
| iOS simulator | pass | pass | pass | pass |
| Android emulator | pass | pass | pass | pass |

`agent-device replay --maestro` did not own the force-quit on this run. On iOS
the Maestro replay daemon reported `SESSION_NOT_FOUND` even after
`agent-device open`. On Android it attached but the Expo developer menu
(`Continue`) covered the probe UI. The probe then `simctl terminate` /
`adb force-stop` and relaunched; that is the kill that the journal resumed.

The scheduled local notification title was not hittable on SpringBoard / the
shade (`wait text "P07 approval"` timed out). After relaunch the deferred was
completed via `lastNotificationData` if present, otherwise
`runtime:eval globalThis.__viviefsP07Complete()`. DurableDeferred completion
after resume is what the check measured; a visible banner tap was not.
