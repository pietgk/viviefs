# P01 platform on SDK 58 — 2026-09-20

Written 2026-09-22 from the ledger pass and `.artifacts/qualification/p01`.
This note records that run. The same review marked ADR-0003 and ADR-0004
Qualified. Neither is Accepted.

Ledger: pass at `2026-09-20T13-34-27.833Z-a195ecaf`, recorded
`2026-09-20T13:37:41.354Z`, commit `aa5e3dd4`, dirty tree.
Bulky artifacts: `.artifacts/qualification/p01` (gitignored).

## Pin

- Expo `58.0.0-preview.3`. Local Xcode 26.6 (build 17F113). Simulator runtime iOS 26.5.
- Crypto polyfill: `expo-crypto` installed onto `globalThis.crypto` (`libs/platform-native/src/crypto-polyfill.ts`).
- `react-native-quick-crypto` was not the polyfill on this run.

## iOS 27

Recorded meaning: build SDK, not the minimum deployment target. Local Xcode 26.6 / iOS 26.x simulators compiled and ran SDK 58. `app.json` leaves the deployment target unset; Expo's default applies at prebuild. Scene-based lifecycle is already required by the Expo template.

## Bundle

iOS `expo export` produced a Hermes `.hbc`.

| Export | Bytes |
| --- | --- |
| Baseline, no SQL import | 3389852 |
| Import of `effect/unstable/sql` (Migrator babel workaround) | 3653396 |

The SQL export completed, so the Migrator import built.

## Checks

Fifteen checks on the dev client, driven by `@expo/agent-cli` `runtime:eval`. With the polyfill, every check passed on the iOS simulator and the Android emulator.

Without the polyfill, both platforms failed the same three checks: `uuid (getRandomValues)`, `subtle.digest direct`, `workflow memory`. The positive control requires `workflow memory` to fail. The other twelve checks still passed.

## ADR status

ADR-0003 and ADR-0004 are Qualified from this record. They are not Accepted.
