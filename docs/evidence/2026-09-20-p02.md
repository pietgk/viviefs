# P02 SQLite drivers — 2026-09-20

Ledger: pass at `2026-09-20T15-51-08.575Z-40d6f8ab`.
Bulky artifacts: `.artifacts/qualification/p02` (gitignored).

## Pin

- `@op-engineering/op-sqlite@18.2.5` (MIT), workspace override.
- `@effect/sql-sqlite-react-native@4.0.0-rc.116` peer remains `>=17.1.2 <18.0.0`.
- 17.1.3 does not compile on RN 0.88 New Architecture (`RCTCxxBridge` removed in 0.87). 18.2.5 uses `RCTBridgeProxy`. D16 named 17.x; this is the measured pin.

## Checks

Same six checks on every platform and driver: migrate, transaction commit, transaction rollback, eavt by entity, eavt by attribute, index volume (1500 rows, covering index).

| Platform | Official | Fallback (expo-sqlite) |
| --- | --- | --- |
| iOS simulator | pass | pass |
| Android emulator (`viviefs-p01`) | pass | pass |
| web (Playwright Chromium) | pass (`@effect/sql-sqlite-wasm` OPFS) | pass |

Android emulator is required; it passed.

## Web notes

Metro serves the OPFS worker. Expo's `import.meta` polyfill has no `.url` in that worker, and sibling `.wasm` URLs were HTML. The worker sets `__ExpoImportMetaRegistry.url` and fetches `/wa-sqlite.wasm` from `apps/evidence-mobile/public/` (`@effect/wa-sqlite` MIT).
