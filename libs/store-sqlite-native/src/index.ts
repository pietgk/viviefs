/**
 * Named slot for `@viviefs/store-sqlite-native`.
 * Official driver layer: `@effect/sql-sqlite-react-native` on op-sqlite 18.2.5 (P02).
 * Log-store contract (append, HLC, cursor) arrives at P04.
 */
export { sqliteNativeLayer } from './layer.ts'
